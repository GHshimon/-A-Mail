# -A-Mail フロントエンド用の環境変数サンプル。
# 実値は .env.local に置く(gitignore 済み)。
#
# 重要: 秘密情報(メールのアプリパスワード / Gemini API キー)は
# ここにも .env.local にも置かない。それらは Rust 側で Keychain に保存する。
# VITE_ 接頭辞の変数のみがフロント(WebView)に露出する。

# 例: 開発時のログ詳細度など、非機密のフラグのみ
# VITE_LOG_LEVEL=info
